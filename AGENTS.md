# Project Instructions

- After completing any app code change, rebuild and restart `Nibiko` before reporting completion.
- Prefer the project run script when available; otherwise use the narrowest build and launch commands for this SwiftPM macOS app.
